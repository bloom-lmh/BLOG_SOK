# 进程通信

## 何为进程通信

在 Electron 中主进程和渲染进程承担着不同的职责，并且他们之间相互隔离。如果想要实现一些复杂的功能，就必须要他们相互通信协作，即进程间通信（IPC）。

在 Electron 中进程通过开发者定义的通道与模块之间传递消息进行通信。而这个通道就是预加载脚本。

## 进程通信的几种模式

有了预加载脚本我们就能很容易的实现进程通信了，Electron 进程通信有机制模式：

### 模式 1：渲染器到主（单向）

> 一句话概述就是主进程订阅一个事件，预加载脚本暴露这个触发该事件的方法，渲染进程调用这个暴露的方法触发这个事件，主进程接收到事件后执行相应的操作。

要从渲染器进程向主进程发送单向 IPC 消息，您可以使用 `ipcRenderer.sendAPI` 发送消息，然后由 `ipcMain.onAPI` 接收。这种模式通常从网页内容调用主进程 API

::: code-group

```js [main.js] {21-22}
const { app, BrowserWindow, ipcMain } = require('electron/main');
const path = require('node:path');

function handleSetTitle(event, title) {
  const webContents = event.sender;
  const win = BrowserWindow.fromWebContents(webContents);
  win.setTitle(title);
}

function createWindow() {
  const mainWindow = new BrowserWindow({
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
    },
  });

  mainWindow.loadFile('index.html');
}

app.whenReady().then(() => {
  // 订阅消息
  ipcMain.on('set-title', handleSetTitle);
  createWindow();

  app.on('activate', function () {
    if (BrowserWindow.getAllWindows().length === 0) createWindow();
  });
});

app.on('window-all-closed', function () {
  if (process.platform !== 'darwin') app.quit();
});
```

```js [预加载.js] {3-4}
const { contextBridge, ipcRenderer } = require('electron/renderer');

// 将API暴露给渲染器进程
contextBridge.exposeInMainWorld('electronAPI', {
  setTitle: (title) => ipcRenderer.send('set-title', title),
});
```

```html [index.html] {10-11}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <!-- https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP -->
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'" />
    <title>Hello World!</title>
  </head>
  <body>
    Title: <input id="title" />
    <button id="btn" type="button">Set</button>
    <!-- 导入渲染器脚本 -->
    <script src="./renderer.js"></script>
  </body>
</html>
```

```js [渲染器.js] {5-6}
const setButton = document.getElementById('btn');
const titleInput = document.getElementById('title');
setButton.addEventListener('click', () => {
  const title = titleInput.value;
  /* 调用暴露的API， */
  window.electronAPI.setTitle(title);
});
```

:::

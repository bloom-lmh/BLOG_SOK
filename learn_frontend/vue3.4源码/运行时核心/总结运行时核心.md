渲染策略：
虚拟节点就是一个中间层，渲染器是平台无关的，vue 内置了浏览器的渲染器，本质上就是在操作 DOM

`vue`将虚拟节点划分为文本节点、元素节点、组件节点、`Fragment` 节点等四种类型（ShapFlags），在`patch`方法中根据位运算来区分不同类型从而做不同的操作，本质上是策略模式的体现。
在更新的时候做了优化

1. 双端指针确定最新操作边界
2. 最大限度复用 DOM
3. 最长递增子序列确定不用移动的元素

组件：
defineProps 传入组件的属性会进行筛选
插槽就是组件的孩子节点
渲染队列的防抖

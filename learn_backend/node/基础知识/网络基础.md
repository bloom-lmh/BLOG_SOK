---
order: 4
---

# 网络基础

## HTTP 协议

### HTTP 协议简介

HTTP（hypertext transport protocol）协议；中文叫超文本传输协议，是一种基于 TCP/IP 的应用层通信协议，这个协议详细规定了浏览器和万维网服务器之间互相通信的规则。协议中主要规定了两个方面的内容

- 客户端：用来向服务器发送数据，可以被称之为请求报文
- 服务端：向客户端返回数据，可以被称之为响应报文

### HTTP 请求报文

![HTTP 请求报文](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251216171649969.png)

#### 请求行

![请求行](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251216171837694.png)

`URL`(Uniform Resource Locator)：是统一资源定位符

#### 请求头

| 请求头                        | 解释                                                                            |
| :---------------------------- | :------------------------------------------------------------------------------ |
| **Host**                      | 请求的目标主机名（域名和端口）                                                  |
| **Connection**                | 控制网络连接在本次请求/响应后的状态，如 `keep-alive` 或 `close`                 |
| **Cache-Control**             | 指定本次请求和响应遵循的缓存机制，如 `no-cache`、`max-age`                      |
| **Upgrade-Insecure-Requests** | 客户端（浏览器）声明其可以处理并希望优先使用 HTTPS 等加密协议                   |
| **User-Agent**                | 发起请求的客户端（如浏览器、应用）的类型、版本、操作系统等信息                  |
| **Accept**                    | 客户端声明其能够处理的媒体类型（MIME 类型），如 `text/html`，`application/json` |
| **Accept-Encoding**           | 客户端声明其支持的压缩编码格式，如 `gzip`，`deflate`                            |
| **Accept-Language**           | 客户端声明其希望接收的语言，如 `zh-CN`，`en-US`                                 |
| **Cookie**                    | 将之前服务器通过 `Set-Cookie` 发送的 Cookie 信息回传给服务器                    |

#### 请求体

请求体内容的格式是非常灵活的

- GET 请求是空
- POST 请求可以是多种`MIME`类型

### HTTP 响应报文

![响应报文](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251216172331544.png)

#### 响应行

- `HTTP/1.1`：HTTP 协议版本号
- 200：响应状态码 `404 Not Found 500 Internal Server Error` 还有一些状态码，参考： [https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Status](https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Status)
- OK：响应状态描述

#### 响应头

```bash
Cache-Control:缓存控制 private 私有的，只允许客户端缓存数据
Connection 链接设置
Content-Type:text/html;charset=utf-8 设置响应体的数据类型以及字符集,响应体为html，字符集
utf-8
Content-Length:响应体的长度，单位为字节
```

::: tip 提示
由于响应体的内容类型很多，浏览器不知道请求体是什么类型的数据，所以要设置 Content-Type 来让浏览器知道
:::

#### 响应体

响应体内容的类型是非常灵活的，常见的类型有 HTML、CSS、JS、图片、JSON。浏览器事先并不知道响应体内容是什么类型的，浏览器默认视为 HTML 类型，即`Content-Type:text/htmlcharset=utf-8`，所以在设置响应体内容时一定要在请求头标明 `Content-Type` 的类型，这样浏览器才能按对应类型解析

## 查看 HTTP 请求报文和响应报文

### 在浏览器中查看

![在浏览器中查看](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251216172740848.png)

### 使用抓包工具 Fiddler

下载链接：[fiddler](https://www.telerik.com/fiddler)

使用演示：

1. 设置信任证书并且抓取 HTTP 包

![设置信任证书并且抓取HTTP包](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251216172839729.png)

2. 开启代理模式让请求和响应由 Fidder 代理

![开启代理模式让请求和响应由Fidder代理](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251216172914490.png)

3. 查看结果

![查看结果](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251216173001714.png)

## 网页资源

### 静态&动态资源

静态资源是指内容长时间不发生改变的资源，例如图片，视频，CSS 文件，JS 文件，HTML 文件，字体文件等；动态资源是指内容经常更新的资源，比如用户信息等

### 网页资源基本加载过程

网页资源的加载都是循序渐进的，首先获取 `HTML` 的内容， 然后解析 `HTML` 再发送其他资源的请求，如`CSS`，`Javascript`，图片等。理解了这个内容对于后续的学习与成长有非常大的帮助

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="./index.css" />
  </head>
  <body>
    <img src="./image/h5.png" alt="" />
    <script src="./js/index.js"></script>
  </body>
</html>
```

![网页资源基本加载过程](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251217095333576.png)

## GET 和 POST

### GET 和 POST 请求场景

`GET` 请求的情况：

- 在地址栏直接输入 `url` 访问
- 点击 `a` 链接
- `link` 标签引入 `css`
- `script` 标签引入 `js`
- `img` 标签引入图片
- `form` 标签中的 `method` 为 `get` （不区分大小写）
- `ajax` 中的 `get` 请求

`POST` 请求的情况：

- `form` 标签中的 `method` 为 `post`（不区分大小写）
- `AJAX` 的 `post` 请求

### GET 和 POST 请求的区别

- 作用：`GET` 主要用来获取数据，`POST` 主要用来提交数据
- 参数位置：`GET` 带参数请求是将参数后缀到 URL 之后，在地址栏中输入 `url` 访问网站就是 `GET` 请求，`POST` 带参数请求是将参数放到请求体中
- 安全性：`POST` 请求相对 `GET` 安全一些，因为在浏览器中参数会暴露在地址栏
- 请求大小：`GET` 请求大小有限制，一般为 `2K`，而 `POST` 请求则没有大小限制

## 同源策略与跨域

### 什么是同源策略

所谓的同源策略(Same Origin Policy)是一种约定，它是浏览器最核心也是最基本的安全功能。同源策略会阻止一个域的`javascrip`脚本和另一个域的内容进行交互，是用于隔离潜在恶意文件的关键安全机制；关于这一点我们后面会举例说明。如果缺少了同源策略浏览器的安全使用会受到很大的影响。可以说 web 是构建在同源策略基础之上的，浏览器只是针对同源策略的一种实现。

### 什么是跨域

所谓的同源就是指一个请求的 `url` 与当前页面的 `url` 同协议、同域名、同端口，满足这三同就是同源。不满足这三同就是跨域，如下图所示：

![跨域](https://image-bucket-1307756649.cos.ap-chengdu.myqcloud.com/image/20251217095858472.png)

跨域根本原因是由同源策略引起的。当页面在执行一个脚本时会检查访问的资源是否同源，如果非同源，在请求数据的时候浏览器会在控制台报一个异常，提示拒绝访问。注意：跨域限制访问，其实是浏览器的限制，是出于安全考虑的。

::: tip 跨域请求发出去了吗？
跨域并不是请求发不出去，请求能发出去，服务端能收到请求并正常返回结果，只是结果被浏览器拦截了。你可能会疑问明明通过表单的方式可以发起跨域请求，为什么 Ajax 就不会?因为归根结底，跨域是为了阻止用户读取到另一个域名下的内容，`Ajax` 可以获取响应，浏览器认为这不安全，所以拦截了响应。但是表单并不会获取新的内容，所以可以发起跨域请求。同时也说明了跨域并不能完全阻止 `CSRF`，因为请求毕竟是发出去了。
:::

### 跨域限制

对于跨域请求，浏览器会根据同源策略进行限制

1. 不能共享 `Cookie`、`LocalStorage`、`IndexDB`
2. 不能获取 `DOM`
3. `AJAX` 请求不能发送

### 同源策略的让步

几乎任何时候安全性和便利性都是负相关的，追求安全性的时候肯定会对便利性造成负面影响。同样，同源策略提升了 web 前端的安全性，但是却牺牲了 web 扩展上的灵活性。所以，现代浏览器在安全性可用性之间选择了一个平衡点。即在遵循同源策略的基础上，选择性的为同源策略“开放了后门”。例如 img、script、style 等变迁都允许跨域引用资源，严格来说这都是不符合同源要求的。只不过这里只是引用这些资源，并不能读取到这些资源的内容。因此浏览器降低了一点点的安全性，但大大提升了网站布置的灵活性。

### 如何解决跨域

解决跨域通常有以下几种方案

- `JSONP` 方式解决跨域
- `CORS` 方式解决跨域（常见，通常仅需服务端修改即可）
- `Nginx` 反向代理解决跨域（推荐使用，配置简单）
- `WebSocket` 解决跨域
- `PostMessage`方式解决跨域（HTML5 中的 XMLHttpRequest Level 2 中的 API）
